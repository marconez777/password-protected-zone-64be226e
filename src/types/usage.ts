
// Simplified usage types - kept for backward compatibility
export interface Usage {
  id: string;
  user_id: string;
}

export interface PlanLimit {
  id: string;
}

export interface Subscription {
  id: string;
  user_id: string;
}
